spring:
  profiles.active: default
---
spring:
  profiles: default
# server config
server.context-path: /springbootexample
# config properties
foo:
  firstProperty: firstFooProperty
  secondProperty: secondFooProperty
